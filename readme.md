### Inicie a aplicação
    * npm run dev
    * A aplicação estará rodando em http://localhost:5173
    * O frontend já está configurado para utilizar o banco de dados da api após subir os containers da forma descrita no backend.